# 图片恢复修复评审

2026-09-14；固定点为本次工作区 HEAD，包含未跟踪的新增实现和测试。
需求依据：Issue335 回归指南、用户要求当前采用 Codex 接收/恢复时机且避免功能回归。

## Standards

首次发现 1 项 P1：Bun 构建生成 image-codec，但发布 workflow 的 tar/zip 白名单未收录。
违反独立发布产物可用的要求。已补两条归档路径并加入打包契约检查。

独立增量复核原文：

> Standards 增量复核通过，无新增可行动缺陷。
> 发布归档两条路径均包含 image-codec，原 P1 已修复。
> 取消监听在完成和中止时均清理；底层 Promise 已附拒绝处理，取消后不再准备后续图片，
> 共享解码器仍受原超时约束。无新图片时同步返回，调用方条件等待，未引入额外纯文本
> 调度边界。原生协议诊断使用准备后的字节摘要。
> 独立复跑：准备历史及诊断测试 236 项通过，发布打包契约 1 项通过。

## Spec

初始静态追踪未发现功能偏差；增量审查发现 1 项 P2：read/MCP 首次校验尚未响应运行取消，
可能等待挂起的解码器。已将入口改为可取消的作用域校验，并分别注入未完成解码测试。

独立最终复核原文：

> Spec 轴通过，前述 P2 已关闭：read/MCP 首次校验均改用 validateImageBytesInRun，
> 取消会立即结束本次等待，MCP 不再处理后续图片。新增两项测试明确在解码 Promise
> 尚未完成时断言取消返回，覆盖原缺口。
> 本轮限定增量内未发现新的可确认问题。结论基于静态复核代码及测试断言，
> 未独立重跑测试。

Standards：1 项已修复，剩余 0 项；Spec：1 项已修复，剩余 0 项。
完整安装包、macOS/Linux 实机仍属发布验收边界，评审不代替这些检查。

## 第三阶段：独立对齐及最小性复查

固定点：`7b5b1b9ef290123767401dffc606e4a726a7654f`，审查工作区 diff 及新增文件。
用户要求：“再派子 Agent 与 Codex 对齐一下并 review 代码……别引入功能和性能限制”。
三个独立子 Agent 分别检查 Codex 对齐、规范/最小性、消费者兼容与性能。

### Standards

1. P2，`image-serialization.ts` 与 read/MCP 调用处存在重复校验（Duplicated Code 判断题）。
   初次 unverified 不缓存，接着准备字节会立即再解码；真实 Worker 挂起注入观察到两次启动、
   约 20 秒。已改为 `prepareValidatedImageBlock` 同步复用首次结果，并减少一次 Buffer 复制。
2. P2，`image-validation.ts` 的串行解码队列未计入超时。两张不同图片并行挂起的回归测试
   修复前实测 20,006 ms；现排队及解码共用每次检查原有的 10 秒预算，过期继续保留原图。

独立增量复核：无硬规范违反及新增可行动缺陷；无需额外线程池、队列框架或图片数量限制。
SA 每轮仅在最终 Provider 确定后扫描一次；其前面的 reasoning 准备不消费图片，无须重复扫描。
并发时仍可持有多份输入字节及运行快照，不能将时间预算表述为整个队列的常量内存保证。

### Spec

1. P2，read/MCP 重复校验违反“不要引入性能限制”；已复用首次结果，取消、MIME、有效格式、
   未验证原图及原始历史语义经独立静态复核保持。
2. P2，通用 Runner 和 ACP 不消费图片时也预处理完整历史，违反“别引入功能和性能限制”。
   离线自定义回调控制组中，30 个引用 3.15 MiB PNG 的块带来约 150 ms、130.5 MiB RSS 增量，
   仅用于确认不必要的工作，不是普遍耗时/内存结论。
   已移除通用 Runner 隐式作用域；SA/AMA 根据实际 native-api 消费者准备，CLI 跳过。
   原生 AMA 的作用域覆盖整个 idle-yield 循环；直接 SDK Provider 保持按需检查。

独立增量复核：未发现新增实际缺陷。native SA 初始恢复及最终 Provider 确定后准备，
native AMA 连续请求和工具快照保持；未发现历史改写、块身份丢失或新增上传限制。
以上两轴最终复核均为只读静态复核，测试由实现方运行。

第三阶段计数：Standards 2 项 P2 均修复，剩余 0；Spec 2 项 P2 均修复，剩余 0。
两轴有重叠，不合计为四个独立问题。

验证记录：Provider/MCP/诊断 47 文件 1022 项通过；Runner/SA/AMA 扩大回归 187 文件
2043 项通过、21 项既有 todo（两批有重叠，不累计）；源码及测试类型检查、packages/bundle/dts
构建通过；Node 构建产物与发布归档契约 4 项通过。新增边界测试结果见 Issue335 回归指南。
稳定后的边界组 8 文件 / 286 项通过，核心文件合计 83.20% 行、92.15% 分支覆盖；
包含真实 SA CLI→native hook 切换及 AMA 跨 Runner 重入仍只校验一次的测试。
